spring:
  kafka:
    consumer:
      bootstrap-servers: kafka1:9091, kafka2:9092, kafka3:9093
      group-id:
    listener:
      type: BATCH

fcm:
  key:
    path: ${FCM_PATH}
    scope: ${FCM_SCOPE}

server:
  port: 8083